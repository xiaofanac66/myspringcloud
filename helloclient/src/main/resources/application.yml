eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8763
spring:
  application:
    name: serviceHi
  cloud:
    config:
      label: master
      profile: dev
  rabbitmq:
    addresses: localhost
    port: 5672
management:
  security:
    enabled: false

